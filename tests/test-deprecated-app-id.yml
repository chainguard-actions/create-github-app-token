name: Test deprecated app-id input error handling
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run action with deprecated app-id and invalid private key (expect failure)
        id: action
        uses: .
        continue-on-error: true
        with:
          app-id: "654321"
          private-key: "not-a-valid-private-key"
      - name: Assert action failed with invalid credentials
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid private key using deprecated app-id, but outcome was '$OUTCOME'"
            exit 1
          fi
          echo "PASS: action correctly failed when using deprecated app-id with invalid private key"
