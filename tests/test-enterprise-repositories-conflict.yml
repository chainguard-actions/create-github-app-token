name: Test enterprise and repositories conflict error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Generate a test RSA private key
        id: gen-key
        run: |
          KEY=$(openssl genrsa 2048 2>/dev/null)
          echo "key<<EOF" >> $GITHUB_OUTPUT
          echo "$KEY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Run action with enterprise and repositories (expect failure)
        id: action
        uses: .
        with:
          client-id: "Iv1.fake123"
          private-key: ${{ steps.gen-key.outputs.key }}
          enterprise: "my-enterprise"
          repositories: "my-repo"
        continue-on-error: true

      - name: Assert action failed due to conflict
        run: |
          echo "outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" != "failure" ]; then
            echo "FAIL: expected action to fail when enterprise and repositories are both set"
            exit 1
          fi
          echo "PASS: action correctly failed when enterprise and repositories conflict"
